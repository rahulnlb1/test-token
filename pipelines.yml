pipelines:
  - name: pipeline123
    steps:
      - name: step123
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo $builder_api_token
            - docker pull ubuntu:16.04
